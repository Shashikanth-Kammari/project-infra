# project-infra

### Infrastructure as a code main importances:
=========================================

1. Version control --> We can maintaine different infrastruture versions.
    so multiple persons can colloborate to create infra.
2. Same infra use across tthe environments like Dev, QA, Prod, etc.
    working in the Dev environemnt , failed in the Prod it will overcome form this.
    run the same code in all environments that create same infra everywhere. 
3. CRUD -- we can create the configuration, Read the configuration, Update the configuration, Delete the configuration.
4. inventory -- list of resources for the project in terms of infra.
5. dependency -- dependencies automatically resolved while CRUD.
6. cost -- creation and delection takes less time, so cost can be reduced.
7. code reuse --modules you can reuse the infra code created multiple times for different projects.
8. state management --implict advantage(feature)


